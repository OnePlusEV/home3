import { getField, updateField } from "vuex-map-fields";

const state = {
    general: {
        useClock: true
    }
};

const getters = {
};

const actions = {

}

const mutations = {
    updateField,
}


export default {
    state,
    getters,
    actions,
    mutations
};